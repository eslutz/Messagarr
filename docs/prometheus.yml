# Prometheus configuration for Messagarr
global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'messagarr-monitor'

scrape_configs:
  - job_name: 'messagarr'
    static_configs:
      - targets: ['messagarr:8080']
        labels:
          service: 'messagarr'
